<% if flash[:notice]%>
<p class="flash-notice"><font color = "green" size="5"><%= flash[:notice] %></font></p>
<br>
  <% end %>

<% if flash[:erro]%>
<p class="flash-notice"><font color = "red" size="5"><%= flash[:erro] %></font></p>
<br>
  <% end %>

<h1>Análise de Matrículas</h1>

<p>A tabela abaixo apresenta os alunos que fizeram solicitações de estágio em cursos do <%= current_usuario.centro%>.</p>

<table>
  <tr>
    <th>Aluno</th>
    <th>RA</th>
 <!--   <th>Código da disciplina</th> -->
  <!--  <th>Nome da disciplina</th> -->

    <th></th>
    <th></th>
  </tr>

<% @matriculas.each do |matricula| %>
 
  
  <% if filtro_centro(matricula.disciplina_id) %>
      <%if filtro_aluno(matricula.aluno_id) %>
      <tr>
        <td><%= retorna_nome_aluno(matricula.aluno_id)%></td>
        <td><%= retorna_ra(matricula.aluno_id)%></td>
        <td><%= link_to "avançar", :controller => 'matriculas', :action => 'detalhes', :id => matricula.aluno_id%></td>
      </tr>
    <%end%>
  <%end %>
<% end %>
 
</table>

<br />


